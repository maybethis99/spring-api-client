services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        REACT_APP_API_BASE: http://localhost:8189
    container_name: imageboard_frontend
    ports:
      - "3000:80"
    restart: unless-stopped
